// import {Redirect, Route} from 'react-router'

// const AdminRoute = ({isAuthorized, redirectPath, ...routerProps}) => {
//     if(isAuthorized) {
//         return <Route {...routerProps} />
//     }

//     return <Redirect to={{pathname: redirectPath}} />
// }

// export default AdminRoute
